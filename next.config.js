/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdnparap20.paragonrels.com', 'bayut-production.s3.eu-central-1.amazonaws.com'],
      },
}

module.exports = nextConfig
